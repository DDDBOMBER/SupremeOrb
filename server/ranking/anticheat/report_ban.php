<?php 
	$user_id = $_POST["user_id"];

	// get list of offences

	// count offenses

	// if offences > 1 account is untrusted
	// do not notify user

	// if offences > 3 permanent ban
	// notify user they are banned, mark account as banned
 ?>